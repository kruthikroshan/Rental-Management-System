name: Astro

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Template placeholder
        run: |
          echo "Astro workflow scaffold is added."
          echo "Set Astro build command and output directory before deployment."
name: Deploy Node.js to Azure Web App

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Template placeholder
        run: |
          echo "Deploy Node.js to Azure Web App workflow scaffold is added."
          echo "Configure Azure credentials, app name, and package path before production use."
name: GitHub Pages Jekyll

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Template placeholder
        run: |
          echo "GitHub Pages Jekyll workflow scaffold is added."
          echo "Use when publishing a Jekyll site via GitHub Pages."
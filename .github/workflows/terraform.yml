name: Terraform

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Template placeholder
        run: |
          echo "Terraform workflow scaffold is added."
          echo "Configure cloud credentials and backend state settings before apply operations."
name: CodeQL Analysis

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Template placeholder
        run: |
          echo "CodeQL Analysis workflow scaffold is added."
          echo "Enable language matrix and adjust build commands for your repository."
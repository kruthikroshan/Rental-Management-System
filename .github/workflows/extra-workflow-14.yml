name: Extra Workflow 14

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "Extra workflow 14 executed"

name: Stale

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Template placeholder
        run: |
          echo "Stale workflow scaffold is added."
          echo "Customize stale and close thresholds before enabling scheduled runs."
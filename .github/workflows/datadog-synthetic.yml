name: Datadog Synthetic

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Template placeholder
        run: |
          echo "Datadog Synthetic workflow scaffold is added."
          echo "Set Datadog API and APP keys, then map test public IDs before execution."
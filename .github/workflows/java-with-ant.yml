name: Java with Ant

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Template placeholder
        run: |
          echo "Java with Ant workflow scaffold is added."
          echo "Use only if your repository includes an Ant-based Java project."
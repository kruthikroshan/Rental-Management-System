name: mdBook

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Template placeholder
        run: |
          echo "mdBook workflow scaffold is added."
          echo "Use when publishing mdBook docs; set source and output paths."